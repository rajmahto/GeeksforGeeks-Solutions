int countNumberswith4(int n) {
        // code here
        if(n==0) return -1;
        int cnt = 0;
        for(int i=1; i<=n ;i++){
            string numStr = to_string(i);
            if(numStr.find('4') != string::npos) cnt++;
        }
        return cnt;
    }
